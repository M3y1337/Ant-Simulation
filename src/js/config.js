export const Config = {
  // World / visuals
  antCount: 50,
  foodSpawnRadius: 50,
  backgroundColor: "#242424",

  // Rendering toggles
  showHUD: true,
  showObstacles: true,
  showPheromones: true,

  // Ant behaviour
  antSpeed: 10,
  antSight: 100,
  antPheromoneFrequency: 2, // frames between pheromone drops

  // Pheromones
  pheromoneLife: 500,
};
